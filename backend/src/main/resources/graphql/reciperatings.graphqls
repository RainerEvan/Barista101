type RecipeRatings{
    id:ID!
    recipe:Recipes
    account:Accounts
    rating:Int
    body:String
    createdAt:DateTime
}

extend type Query{
    getAllRatingsForRecipe(recipeId:ID!):[RecipeRatings]
}