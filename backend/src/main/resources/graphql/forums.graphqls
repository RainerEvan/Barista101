type Forums{
    id:ID!
    author:Accounts
    body:String
    createdAt:DateTime
}

extend type Query{
    getAllForums:[Forums]
    getForum(forumId:ID!):Forums
    getAllForumsForAccount(accountId:ID!):[Forums]
}