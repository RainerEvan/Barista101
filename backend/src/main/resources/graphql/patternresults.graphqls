type PatternResults{
    id:ID!
    account:Accounts
    pattern:Patterns
    score:Int
    result:String
}

extend type Query{
    getAllPatternResultsForAccount(accountId:ID!):[PatternResults]
    getPatternResult(patternResultId:ID!):PatternResults
}