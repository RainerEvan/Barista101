type Enrollments{
    id:ID!
    account:Accounts
    course:Courses
    startDate:DateTime
    endDate:DateTime
    moduleStatus:String
    progress:Int
}

extend type Query{
    getAllEnrollmentsForAccount(accountId:ID!):[Enrollments]
    getEnrollment(enrollmentId:ID!):Enrollments
}