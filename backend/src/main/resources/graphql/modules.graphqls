type Modules{
    id:ID!
    course:Courses
    title:String
    thumbnail:String
    contents:[Contents]
}

extend type Query{
    getAllModulesForCourse(courseId:ID!):[Modules]
    getModule(moduleId:ID!):Modules
}