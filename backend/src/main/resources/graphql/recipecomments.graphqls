type RecipeComments{
    id:ID!
    recipe:Recipes
    author:Accounts
    body:String
    createdAt:DateTime
    updatedAt:DateTime
}

extend type Query{
    getAllCommentsForRecipe(recipeId:ID!):[RecipeComments]
}